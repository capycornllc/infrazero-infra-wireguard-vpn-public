#!/usr/bin/env bash

# Set this to the provider directory name.
export INFRAZERO_PROVIDER="replace-me"

provider_route_mode() {
  echo "none"
}

provider_outbound_defaults() {
  return 0
}

provider_detect_public_iface() {
  ip route show default 2>/dev/null | awk '{print $5; exit}'
}

provider_admin_useradd_options() {
  return 0
}
